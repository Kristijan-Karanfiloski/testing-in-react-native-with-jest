import React from "react";
import { styleQueries } from "../helperFunctions/styleQueries";

const useStyleQueries = () => {
  // return {};
  return { myComponent: { fontSize: 16 } };
};

export default useStyleQueries;

export const multiplyTwoNumbers = (a, b) => {
  return a * b;
};
